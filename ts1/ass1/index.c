#include <stdio.h>
int main()
{
   // int number;
   // printf("enter the number: ");
   // scanf("%d", &number);
   // if (number % 3 == 0 && number % 5 == 0)
   // {
   //     printf("this number divid by 3&5 ");
   // }
   // else
   // {
   //     printf("this number not divid ");
   // }
   //-----------------------------------------
   // int number;
   // printf("enter your number :");
   // scanf("%d", &number);
   // if (number % 2 == 0)
   // {
   //     printf("this number is even");
   // }
   // else
   // {
   //     printf("this number from the kes");
   // }
   //-----------------------------------------
   // char n;
   // int hwm, rw, nbc, s;
   // printf("enter the first later of your name:");
   // scanf("%c", &n);
   // printf("enter your hour work per month:");
   // scanf("%d", &hwm);
   // printf("enter your rate per hour:");
   // scanf("%d$", &rw);
   // printf("enter number of your children:");
   // scanf("%d", &nbc);
   // s = hwm * rw + nbc * 20;
   // printf("your first later is: %c\n", n);
   // printf("sallary in month is :%d$\n", s);
   // -----------------------------------------
   // int mathe, ph, co, bi, ch, avareg;
   // printf("enter number of the mathe:");
   // scanf("%d", &mathe);
   // printf("enter number of the ph:");
   // scanf("%d", &ph);

   // printf("enter number of the co:");
   // scanf("%d", &co);
   // printf("enter number of the bi:");
   // scanf("%d", &bi);
   // printf("enter number of the ch:");
   // scanf("%d", &ch);
   // avareg = (mathe + ph + co + bi + ch) / 5;
   // if (avareg >= 90)
   // {
   //    printf("the avareg is A");
   // }
   // else if (avareg >= 80)
   // {
   //    printf("the avareg is B ");
   // }
   // else if (avareg >= 70)
   // {
   //    printf("the avareg is C");
   // }
   // else if (avareg >= 60)
   // {
   //    printf("the avareg is D");
   // }
   // else
   // {
   //    printf("the avareg is F");
   // }
}
